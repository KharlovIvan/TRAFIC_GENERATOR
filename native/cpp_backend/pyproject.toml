[build-system]
requires = ["scikit-build-core>=0.5", "pybind11>=2.11"]
build-backend = "scikit_build_core.build"

[project]
name = "trafic-native"
version = "0.1.0"
description = "C++ native sender backend for TRAFIC_GENERATOR"
requires-python = ">=3.12"
